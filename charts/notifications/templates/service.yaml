apiVersion: v1
kind: Service
metadata:
  name: notifications
spec:
  selector:
    app: notifications
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
  type: {{ .Values.service.type }}
